import bus from '../bus.js';

const GameEngine = () => {
  const state = {
    tiles: [[]],
    gems: [],
    characters: [],
    tick: 0,
  };

  const loadLevel = (level) => {
    // populate tiles from level map
  };
};

export default GameEngine;